---
- name: Patch VM CPU cores in Gitea Infrastructure manifest
  hosts: localhost
  gather_facts: false
  vars_files:
    - defaults/main.yml
  tasks:
    - name: Patch VM manifest CPU resources
      ansible.builtin.import_role:
        name: vm_manifest
        tasks_from: patch_cpu.yml
