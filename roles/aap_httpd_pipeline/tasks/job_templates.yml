---
- name: Ensure Install httpd job template exists in AAP
  awx.awx.job_template:
    name: "{{ aap_httpd_pipeline_jt_install }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    project: "{{ aap_httpd_pipeline_project }}"
    inventory: "{{ aap_httpd_pipeline_inventory }}"
    playbook: "{{ aap_httpd_pipeline_playbook_install }}"
    job_type: run
    state: present
    survey_enabled: false
    extra_vars: "{{ aap_httpd_pipeline_job_extra_vars }}"
    ask_variables_on_launch: true
    credentials:
      - "{{ aap_httpd_pipeline_credential_vms }}"
      - "{{ aap_httpd_pipeline_credential_itsm }}"
      - "{{ aap_httpd_pipeline_credential_aap }}"
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Ensure Deploy Apache App job template exists in AAP
  awx.awx.job_template:
    name: "{{ aap_httpd_pipeline_jt_deploy }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    project: "{{ aap_httpd_pipeline_project }}"
    inventory: "{{ aap_httpd_pipeline_inventory }}"
    playbook: "{{ aap_httpd_pipeline_playbook_deploy }}"
    job_type: run
    state: present
    survey_enabled: false
    extra_vars: "{{ aap_httpd_pipeline_job_extra_vars }}"
    ask_variables_on_launch: true
    credentials:
      - "{{ aap_httpd_pipeline_credential_vms }}"
      - "{{ aap_httpd_pipeline_credential_itsm }}"
      - "{{ aap_httpd_pipeline_credential_aap }}"
      - "{{ aap_httpd_pipeline_credential_gitea }}"
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Ensure Expose Apache job template exists in AAP
  awx.awx.job_template:
    name: "{{ aap_httpd_pipeline_jt_expose }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    project: "{{ aap_httpd_pipeline_project }}"
    inventory: "{{ aap_httpd_pipeline_inventory }}"
    playbook: "{{ aap_httpd_pipeline_playbook_expose }}"
    job_type: run
    state: present
    survey_enabled: false
    extra_vars: "{{ aap_httpd_pipeline_job_extra_vars }}"
    ask_variables_on_launch: true
    credentials:
      - "{{ aap_httpd_pipeline_credential_openshift }}"
      - "{{ aap_httpd_pipeline_credential_gitea }}"
      - "{{ aap_httpd_pipeline_credential_itsm }}"
      - "{{ aap_httpd_pipeline_credential_aap }}"
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Ensure Register ITSM Apache Application Asset job template exists in AAP
  awx.awx.job_template:
    name: "{{ aap_httpd_pipeline_jt_register_itsm }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    project: "{{ aap_httpd_pipeline_project }}"
    inventory: "{{ aap_httpd_pipeline_inventory }}"
    playbook: "{{ aap_httpd_pipeline_playbook_register_itsm }}"
    job_type: run
    state: present
    survey_enabled: false
    extra_vars: "{{ aap_httpd_pipeline_job_extra_vars }}"
    ask_variables_on_launch: true
    credentials:
      - "{{ aap_httpd_pipeline_credential_vms }}"
      - "{{ aap_httpd_pipeline_credential_openshift }}"
      - "{{ aap_httpd_pipeline_credential_itsm }}"
      - "{{ aap_httpd_pipeline_credential_aap }}"
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Ensure Start httpd job template exists in AAP
  awx.awx.job_template:
    name: "{{ aap_httpd_pipeline_jt_start }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    project: "{{ aap_httpd_pipeline_project }}"
    inventory: "{{ aap_httpd_pipeline_inventory }}"
    playbook: "{{ aap_httpd_pipeline_playbook_start }}"
    job_type: run
    state: present
    survey_enabled: false
    extra_vars: "{{ aap_httpd_pipeline_job_extra_vars }}"
    ask_variables_on_launch: true
    credentials:
      - "{{ aap_httpd_pipeline_credential_vms }}"
      - "{{ aap_httpd_pipeline_credential_itsm }}"
      - "{{ aap_httpd_pipeline_credential_aap }}"
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"
