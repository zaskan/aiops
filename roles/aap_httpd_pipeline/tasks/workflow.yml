---
- name: Ensure Deploy Apache App workflow job template exists in AAP
  awx.awx.workflow_job_template:
    name: "{{ aap_httpd_pipeline_workflow }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    inventory: "{{ aap_httpd_pipeline_inventory }}"
    survey_enabled: true
    survey_spec: "{{ aap_httpd_pipeline_workflow_survey_spec }}"
    extra_vars: "{{ aap_itsm_subworkflow_extra_vars | combine(aap_httpd_survey_job_extra_vars) }}"
    ask_variables_on_launch: true
    state: present
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Ensure Start httpd workflow node exists
  awx.awx.workflow_job_template_node:
    identifier: start_httpd
    workflow_job_template: "{{ aap_httpd_pipeline_workflow }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    unified_job_template: "{{ aap_httpd_pipeline_jt_start }}"
    extra_data:
      itsm_change_ref: "{{ '{{ itsm_change_ref }}' }}"
      itsm_service_request_ref: "{{ '{{ itsm_service_request_ref | default(\"\") }}' }}"
      itsm_ctask_template_name: "{{ itsm_apache_stack_task_template_start_httpd }}"
      target_host: "{{ '{{ vm_name | default(target_host) }}' }}"
      vm_name: "{{ '{{ vm_name | default(target_host) }}' }}"
      app_repo: "{{ '{{ app_repo }}' }}"
      app_branch: "{{ '{{ app_branch | default(\"main\") }}' }}"
    success_nodes: []
    state: present
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Ensure Register ITSM Apache Application Asset workflow node exists
  awx.awx.workflow_job_template_node:
    identifier: register_itsm_apache_app_asset
    workflow_job_template: "{{ aap_httpd_pipeline_workflow }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    unified_job_template: "{{ aap_httpd_pipeline_jt_register_itsm }}"
    extra_data:
      target_host: "{{ '{{ vm_name | default(target_host) }}' }}"
      vm_name: "{{ '{{ vm_name | default(target_host) }}' }}"
      app_repo: "{{ '{{ app_repo }}' }}"
      app_branch: "{{ '{{ app_branch | default(\"main\") }}' }}"
    success_nodes:
      - start_httpd
    state: present
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Ensure Expose Apache workflow node exists
  awx.awx.workflow_job_template_node:
    identifier: expose_apache
    workflow_job_template: "{{ aap_httpd_pipeline_workflow }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    unified_job_template: "{{ aap_httpd_pipeline_jt_expose }}"
    extra_data:
      itsm_change_ref: "{{ '{{ itsm_change_ref }}' }}"
      itsm_service_request_ref: "{{ '{{ itsm_service_request_ref | default(\"\") }}' }}"
      itsm_ctask_template_name: "{{ itsm_apache_stack_task_template_expose_apache }}"
      target_host: "{{ '{{ vm_name | default(target_host) }}' }}"
      vm_name: "{{ '{{ vm_name | default(target_host) }}' }}"
      app_repo: "{{ '{{ app_repo }}' }}"
      app_branch: "{{ '{{ app_branch | default(\"main\") }}' }}"
    success_nodes:
      - register_itsm_apache_app_asset
    state: present
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Clear Deploy Apache App workflow node edges before re-linking
  awx.awx.workflow_job_template_node:
    identifier: deploy_apache_app
    workflow_job_template: "{{ aap_httpd_pipeline_workflow }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    unified_job_template: "{{ aap_httpd_pipeline_jt_deploy }}"
    extra_data:
      itsm_change_ref: "{{ '{{ itsm_change_ref }}' }}"
      itsm_service_request_ref: "{{ '{{ itsm_service_request_ref | default(\"\") }}' }}"
      itsm_ctask_template_name: "{{ itsm_apache_stack_task_template_deploy_app }}"
      target_host: "{{ '{{ vm_name | default(target_host) }}' }}"
      vm_name: "{{ '{{ vm_name | default(target_host) }}' }}"
      app_repo: "{{ '{{ app_repo }}' }}"
      app_branch: "{{ '{{ app_branch | default(\"main\") }}' }}"
    success_nodes: []
    state: present
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Clear Install httpd workflow node edges before re-linking
  awx.awx.workflow_job_template_node:
    identifier: install_httpd
    workflow_job_template: "{{ aap_httpd_pipeline_workflow }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    unified_job_template: "{{ aap_httpd_pipeline_jt_install }}"
    extra_data:
      itsm_change_ref: "{{ '{{ itsm_change_ref }}' }}"
      itsm_service_request_ref: "{{ '{{ itsm_service_request_ref | default(\"\") }}' }}"
      itsm_ctask_template_name: "{{ itsm_apache_stack_task_template_install_httpd }}"
      target_host: "{{ '{{ vm_name | default(target_host) }}' }}"
      vm_name: "{{ '{{ vm_name | default(target_host) }}' }}"
      app_repo: "{{ '{{ app_repo }}' }}"
      app_branch: "{{ '{{ app_branch | default(\"main\") }}' }}"
    success_nodes: []
    state: present
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Ensure Deploy Apache App workflow node links to Expose Apache
  awx.awx.workflow_job_template_node:
    identifier: deploy_apache_app
    workflow_job_template: "{{ aap_httpd_pipeline_workflow }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    unified_job_template: "{{ aap_httpd_pipeline_jt_deploy }}"
    extra_data:
      itsm_change_ref: "{{ '{{ itsm_change_ref }}' }}"
      itsm_service_request_ref: "{{ '{{ itsm_service_request_ref | default(\"\") }}' }}"
      itsm_ctask_template_name: "{{ itsm_apache_stack_task_template_deploy_app }}"
      target_host: "{{ '{{ vm_name | default(target_host) }}' }}"
      vm_name: "{{ '{{ vm_name | default(target_host) }}' }}"
      app_repo: "{{ '{{ app_repo }}' }}"
      app_branch: "{{ '{{ app_branch | default(\"main\") }}' }}"
    success_nodes:
      - expose_apache
    state: present
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"

- name: Ensure Install httpd workflow node links to Deploy Apache App
  awx.awx.workflow_job_template_node:
    identifier: install_httpd
    workflow_job_template: "{{ aap_httpd_pipeline_workflow }}"
    organization: "{{ aap_httpd_pipeline_organization }}"
    unified_job_template: "{{ aap_httpd_pipeline_jt_install }}"
    extra_data:
      itsm_change_ref: "{{ '{{ itsm_change_ref }}' }}"
      itsm_service_request_ref: "{{ '{{ itsm_service_request_ref | default(\"\") }}' }}"
      itsm_ctask_template_name: "{{ itsm_apache_stack_task_template_install_httpd }}"
      target_host: "{{ '{{ vm_name | default(target_host) }}' }}"
      vm_name: "{{ '{{ vm_name | default(target_host) }}' }}"
      app_repo: "{{ '{{ app_repo }}' }}"
      app_branch: "{{ '{{ app_branch | default(\"main\") }}' }}"
    success_nodes:
      - deploy_apache_app
    state: present
    controller_host: "{{ aap_httpd_pipeline_controller_host }}"
    controller_oauthtoken: "{{ aap_httpd_pipeline_controller_oauthtoken }}"
    validate_certs: "{{ aap_httpd_pipeline_validate_certs }}"
