---
- name: Deploy Loki ConfigMap, Deployment, and Service
  kubernetes.core.k8s:
    state: present
    definition: "{{ item }}"
  loop: "{{ lookup('template', 'loki_stack.yaml.j2') | from_yaml_all | list }}"
