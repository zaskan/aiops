---
- name: Obtain AAP gateway OAuth token for awx.awx modules
  ansible.builtin.uri:
    url: "{{ aap_apache_troubleshoot_pipeline_controller_host }}/api/gateway/v1/tokens/"
    method: POST
    user: "{{ aap_apache_troubleshoot_pipeline_controller_username }}"
    password: "{{ aap_apache_troubleshoot_pipeline_controller_password }}"
    force_basic_auth: true
    validate_certs: "{{ aap_apache_troubleshoot_pipeline_validate_certs }}"
    body_format: json
    body:
      description: aap-apache-troubleshoot-pipeline ansible automation
      scope: write
    status_code: 201
  register: aap_apache_troubleshoot_pipeline_gateway_token_create
  no_log: true

- name: Set AAP controller OAuth token fact
  ansible.builtin.set_fact:
    aap_apache_troubleshoot_pipeline_controller_oauthtoken: "{{ aap_apache_troubleshoot_pipeline_gateway_token_create.json.token }}"
  no_log: true
